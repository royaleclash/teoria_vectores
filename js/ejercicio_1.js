//crear un vector vacio llamado "colores"
//luego agrega 3 colores diferentes y recorre el vector 

let colores = [] 

colores.push = ["azul", "verde", "rojo"];

console.log (colores);


for (let i = 0; i < colores.length; i++ ) {
    console.log (colores [i])
};